<h1>Dogs</h1>

<%= button_to "Sort by most popular", dogs_path, method: :get, params: { sort: true } %>

<% @dogs.each do |dog| %>
    <h3><%= link_to dog.name,dog %></h3>
    <p>Age: <%= dog.age %></p>
    <p>Breed: <%= dog.breed %></p>
<% end %>